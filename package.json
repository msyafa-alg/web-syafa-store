{
  "name": "web-syafa-store",
  "version": "1.0.0",
  "description": "Professional Payment Gateway for Pterodactyl Servers",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "dev-watch": "npx nodemon index.js",
    "reset-storage": "node reset-storage.js",
    "setup": "npm install && node reset-storage.js",
    "test": "echo \"Running health check...\" && curl http://localhost:3000/api/health || echo \"Server not running\""
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "axios": "^1.6.8",
    "body-parser": "^1.20.2",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}